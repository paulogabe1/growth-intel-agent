# Inherits GROQ_API_KEY, GROQ_MODEL, SUPABASE_URL, SUPABASE_KEY from
# the shared ../.env. Only set something here if it needs to differ,
# e.g. a faster, smaller model just for research queries:

# GROQ_MODEL=groq/llama-3.1-8b-instant
